# Add project-specific ProGuard/R8 keep rules here.
#
# The Flutter Gradle plugin and plugins contribute their own consumer rules
# automatically, so this file is usually empty. Add -keep rules only when a
# release (minified) build strips or renames something accessed via reflection,
# JNI, or serialization.
