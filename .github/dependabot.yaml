version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
  # pub upgrades are handled by the AI Dependency Upgrade workflow, which can also
  # apply breaking-change migrations. Re-enable this block to hand pub bumps back
  # to Dependabot.
  # - package-ecosystem: "pub"
  #   directory: "/"
  #   schedule:
  #     interval: "daily"
